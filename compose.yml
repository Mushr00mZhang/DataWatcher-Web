services:
  interfacewatcher-web:
    image: interfacewatcher-web
    container_name: interfacewatcher-web
    ports:
      - 3000:80
    networks:
      - common_bridge
networks:
  common_bridge:
    external: true
